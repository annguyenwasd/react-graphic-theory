const colors = {
  white: [255],
  selected: [255, 0, 100, 200],
  hover: [255, 0, 100, 100]
};

const tools = {
  VERTEX: 'VERTEX',
  UNDIRECTIONAL: 'UNDIRECTIONAL',
  DIRECTIONAL: 'DIRECTIONAL',
  LOOP: 'LOOP'
};

const graphType = tools;

export { colors, tools, graphType };
